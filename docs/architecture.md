# 🧩 System Architecture Overview

Цей документ описує архітектуру системи **Course Recommendation Platform**, що складається з двох основних частин:
- **Backend (C++ / Crow REST API)** — ядро логіки, бази даних і рекомендаційних алгоритмів.
- **Frontend (Python / PyQt)** — десктопний клієнт для взаємодії з користувачем.

---

## 1. Загальна концепція

Система призначена для формування персоналізованих навчальних планів на основі профілю користувача. Архітектура побудована за принципами **модульності**, **інтерфейсного розділення** і **інверсії залежностей**.

```text
[ User (PyQt App) ] ⇄ [ Crow REST API (C++) ] ⇄ [ JSON/SQLite Storage ]
```

Користувач вводить свої цілі, інтереси, рівень і доступний час → фронтенд надсилає запит через REST API → бекенд формує персональний план → повертає його у JSON-форматі.

---

## 2. Компонентна діаграма

| Компонент | Мова / Технологія | Призначення |
|------------|------------------|--------------|
| **Crow Server** | C++20, Crow | REST API, маршрути `/api/...` |
| **Catalog Module** | C++ | Робота з курсами (ICatalog, JsonCatalog, SqliteCatalog) |
| **Storage Module** | C++ | Збереження планів користувачів (IStorage, JsonStorage) |
| **Recommender Module** | C++ | Алгоритми побудови планів (IRecommenderStrategy, Greedy, DP) |
| **Scoring Service** | C++ | Оцінка релевантності курсів (інтереси, рівень, час) |
| **Frontend App** | Python, PyQt5 | Графічний інтерфейс, візуалізація планів, запити до API |

---

## 3. Потік даних (Data Flow)

1. **Користувач** вводить профіль (інтереси, рівень, час).
2. **PyQt App** відправляє POST `/api/recommendations`.
3. **Crow Server** отримує запит і викликає:
   - `ICatalog.getAll()` для отримання курсів;
   - `IRecommenderStrategy.makePlan()` для побудови плану;
   - `IStorage.savePlan()` для збереження результату.
4. План повертається у форматі JSON.
5. **Frontend** відображає план у вигляді таймлайну/таблиці.

---

## 4. Основні інтерфейси

- **ICatalog** — постачальник списку курсів (з JSON або БД).
- **IStorage** — інтерфейс для збереження/завантаження навчальних планів.
- **IRecommenderStrategy** — стратегія побудови плану (Greedy, DP, ML тощо).
- **ScoringService** — обчислення рейтингу курсів за профілем користувача.

---

## 5. Взаємодія Frontend ↔ Backend

| Етап | HTTP метод | Endpoint | Опис |
|------|-------------|-----------|------|
| Ініціалізація | GET | `/api/courses` | Отримання каталогу курсів |
| Рекомендація | POST | `/api/recommendations` | Побудова плану |
| Збереження | POST | `/api/plans/:userId` | Збереження плану |
| Завантаження | GET | `/api/plans/:userId` | Завантаження плану |

---

## 6. Розширення системи

- **SQLiteStorage / SQLiteCatalog** — альтернатива JSON для продуктивності.
- **ML-модуль** — інтеграція AI-рекомендера через Python API.
- **JWT/Auth** — можливість додати авторизацію користувачів.
- **Web Dashboard** — підключення веб-версії без зміни ядра.

---

## 7. Нефункціональні вимоги
- Підтримка багатопоточної обробки запитів (Crow).
- Легка розширюваність: додавання нових стратегій без зміни API.
- Переносимість (Windows/Linux).
- Залежності мінімальні: Crow, nlohmann/json, PyQt.

---

## 8. Візуальна схема (логічна)
```text
Frontend (PyQt)
 ├── ProfileForm
 ├── CourseListView
 └── PlanVisualizer
        ↓
     REST API (Crow)
        ↓
  ┌────────────────────────────┐
  │ Catalog  │ Storage │ Recommender │ Scoring │
  └────────────────────────────┘
        ↓
     JSON / SQLite files
```

---

Ця архітектура створює збалансовану систему, де C++ відповідає за швидкість і логіку, а Python — за UX і інтеграцію з користувачем.