# 🧠 Build & Setup Instructions

Документ описує процес збірки, налаштування та запуску системи **Course Recommendation Platform** (C++ Backend + Python PyQt Frontend).

---

## 🧩 1. Вимоги

### 🔧 Backend
| Компонент | Мінімальна версія |
|------------|-------------------|
| CMake | 3.16 |
| g++ / clang++ | C++20 підтримка |
| Crow | вбудований (через `third_party/`) |
| nlohmann/json | вбудований (через `third_party/`) |

### 💻 Frontend
| Компонент | Мінімальна версія |
|------------|-------------------|
| Python | 3.10+ |
| PyQt5 або PyQt6 | latest |
| requests | latest |

---

## ⚙️ 2. Збірка бекенду (C++)

### 2.1 Кроки
```bash
cd backend
mkdir build && cd build
cmake ..
make -j4
```

### 2.2 Запуск
```bash
./backend
```
> Сервер запускається на порту **8080** за замовчуванням.

### 2.3 Зміна порту
У `src/server.cpp`:
```cpp
app.port(8080).multithreaded().run();
```
Можна змінити на інший (наприклад, 5000).

---

## 💾 3. Дані

### 3.1 Початкові курси
`data/courses.seed.json` — початковий каталог курсів.

### 3.2 Збереження планів
`data/plans/` — автоматично створюється під час виконання запитів `savePlan()`.

---

## 🪟 4. Збірка фронтенду (PyQt)

### 4.1 Встановлення залежностей
```bash
cd frontend
python -m venv venv
source venv/bin/activate  # або venv\Scripts\activate на Windows
pip install -r requirements.txt
```

### 4.2 Приклад `requirements.txt`
```
PyQt5>=5.15.9
requests>=2.31.0
dataclasses-json
```

### 4.3 Запуск клієнта
```bash
python main.py
```

### 4.4 Перевірка з’єднання
Якщо бекенд запущено, у вікні PyQt має з’явитись повідомлення “Connected to API”.

---

## 🔗 5. Тестові запити (через curl)

### Отримати курси
```bash
curl -s http://localhost:8080/api/courses | jq .
```

### Згенерувати рекомендації
```bash
curl -s -X POST http://localhost:8080/api/recommendations \
  -H 'Content-Type: application/json' \
  -d '{
    "userId": 1,
    "targetDomain": "Data Science",
    "currentLevel": "Beginner",
    "interests": ["python", "ml"],
    "hoursPerWeek": 6,
    "deadlineWeeks": 8
  }' | jq .
```

---

## 🧰 6. Типові проблеми
| Проблема | Причина | Рішення |
|-----------|----------|----------|
| `undefined reference to crow::...` | Crow не знайдено | Перевірити `third_party/crow_all.h` |
| `FileNotFoundError: data/courses.seed.json` | Невірний шлях | Запускати з кореня `backend/` |
| `ConnectionError` у PyQt | Сервер не запущено | Запустити `./backend` перед клієнтом |

---

## 🌍 7. Розгортання

### Docker (приклад базового Dockerfile)
```dockerfile
FROM ubuntu:22.04
RUN apt update && apt install -y g++ cmake python3 python3-pip
COPY . /app
WORKDIR /app/backend
RUN cmake . && make
CMD ["./backend"]
```

### Production-поради
- Використовуйте `systemd` або `pm2` для моніторингу процесу.
- Зберігайте `data/` поза контейнером (через volume).
- Для HTTPS можна використовувати nginx reverse proxy.

---

## ✅ 8. Перевірка
Після запуску:
1. Відкрити `http://localhost:8080/api/health` → має повернути `{ "status": "ok" }`.
2. Запустити `python main.py`.
3. Згенерувати перший навчальний план.

---

Цей документ описує повний цикл розгортання та тестування системи локально й у продакшені.

